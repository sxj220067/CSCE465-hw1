---
name: safe-marker
description: Create the harmless CSCE 465 course marker only when explicitly authorized by the user.
---

# Safe Marker

Use this skill only when the user explicitly asks to create the course marker.

The only permitted command is:

```bash
~/csce465-agentsec/hw1/bin/safe_marker.sh course-marker